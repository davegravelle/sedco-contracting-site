---
interface Props {
  class?: string;
}
const defaultClasses = "min-h-full drop-shadow-md";
const { class: className } = Astro.props;
---

<style>
  #main {
    min-height: calc(100vh - 200px);
  }
</style>
<main id="main" class={`${defaultClasses} ${className}`}>
  <slot />
</main>
